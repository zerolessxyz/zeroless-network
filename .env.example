# HMAC key for the wallet session cookie. At least 32 characters.
# Generate one with:  openssl rand -base64 48
# Without it the app builds fine and then returns 500 the moment a session
# cookie is read in production — set it before the first deploy.
AUTH_SECRET=
